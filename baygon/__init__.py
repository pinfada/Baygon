"""Baygon — a lightweight orchestration layer.

Baygon turns a user intention into an execution plan and delegates every
action to interchangeable provider implementations. The core contains no
business logic and knows no provider: it only knows capabilities.
"""

__version__ = "0.1.0"
